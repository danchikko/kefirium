import styled from "styled-components";

const Navbar = () => {
    return (
        <Container>

        </Container>
    )
}

const Container = styled.div`
    display: flex;
`

export default Navbar;